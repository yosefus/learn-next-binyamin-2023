// "use client"

// import { useSearchParams } from "next/navigation"

export default function page({ searchParams }) {
   // const searchP = useSearchParams()
   // console.log(searchP.get('searchby'));

   console.log(searchParams);


   return (
      <div>

      </div>
   )
}
